# vulns-news

## Platform

Webアプリケーション。

## Stack

同一リポジトリ内のfrontend/にVue + TypeScript、バックエンドにGoを使う。

## Users

PSIRT、メンテナー、脅威インテリジェンスなどのセキュリティ従事者。

## Product Purpose

一般的な脆弱性情報と、公開リポジトリに関連する情報を閲覧し、影響・根拠・対応方針を確認する。

## Capabilities and Constraints

採用したデザインを基に開発を続けるフロントエンド。画面、入力検証、状態管理、ナビゲーションを実装し、取得・解析・認証・永続化の未接続部分はローカルのアダプターで動作させる。実APIへの移行ではこれらの境界を置き換え、画面全体を作り直す前提にはしない。

一般・リポジトリ別フィード、検索、並び替え、CVSS、リポジトリへの影響と対応優先度、記事ページ、複数リポジトリ設定、プロフィール操作、保存、コメント、対応状況を含む。PoCは静的な文字列として読み、実行しない。

処理段階と分析結果を分ける。未確定の記事には確定した分析本文や対応手順を表示しない。失敗や再解析の途中でも、既に取得できた記事と既存レポートを閲覧できるようにする。

CVE・GHSA・公開アドバイザリURLの入力、既存レポートの再利用、新規依頼、再分析、期限付き追跡、過去情報を含むリポジトリ検索、共有をフル版で試せる。このうち「脆弱性情報版VirusTotal」として追加した個別解析・追跡の構想はチーム未合意。検討用の機能として残し、合意前に本番要件やバックエンドの実装義務として扱わない。

追跡7日・確認間隔1日は暫定値。期限終了後も既存レポートは読める。手動の再分析だけでは追跡期限を延長しない。新着の追加候補は利用者が反映するまで閲覧中の一覧を変えない。

MVP表示は同じ部品から、閲覧、検索、重要度・関連度順、単一リポジトリ入力、記事詳細を提供する。フル版のコードを削除したり、実装をコピーして分岐させたりしない。

取得・LLM解析・認証・公開コメント・DB永続化は、完成した機能として接続されていない。ゲスト状態はsessionStorage、ローカルプロフィールはlocalStorageに保持し、本人確認や認可には使わない。プロフィール切替・ログアウト・保存失敗時も、別の利用者のデータへ書き込まないよう境界を保つ。

## Brand Personality

白・黒・グレーに青を組み合わせ、文字と余白で情報を整理する。「カンザキイオリ／amazarashi」のモチーフは抽象的な文字の使い方にとどめ、歌詞・ロゴ・写真・既存レイアウトは流用しない。

可用性と利便性を優先する。本文・入力16px、一覧タイトル18px、補助情報14pxを基本とする。左の一覧は通常の文書スクロール、右の詳細は操作と見出しを残して本文だけをスクロールする。具体的な配色、余白、文字、レスポンシブ表示は [DESIGN.md](DESIGN.md) に定める。

## Evidence on Hand

標準フィードと過去レポートは固定の架空データ。リポジトリ別表示は固定プロフィールを使い、実リポジトリや過去の全情報を取得していない。新規入力の存在・製品・バージョン・CVSSは、ローカル処理が完了しても未確認・未評価のままとする。

固定状態を切り替えるuseAnalysisPreviewと、時間経過で操作を再現するuseReportLibraryは、検証用のローカル処理。定期確認は画面を開いている間だけ動き、サーバーの定期ジョブではない。localhostやタブ内だけに存在するレポートのURLを、他端末から閲覧できる共有サービスとして扱わない。

バックエンドの各ブランチで確認できたAPIと不足項目、未合意の提案は [連携メモ](docs/frontend-integration.md) に記録する。型宣言だけで接続済み・解析済みとは判断せず、実行結果と静的な確認を分ける。

## Product Principles

- CVSS、リポジトリ関連度、対応優先度、分析の確度、分析済みかどうかを混同しない。
- 不明な件数・進捗率・関連度を推測して埋めない。
- 記事本文・コメント・LLM出力・外部入力を信頼せず、生HTMLとして表示しない。
- UI、入力検証、状態保存、データ取得を分け、レビューで責務を追える構造にする。
- 未合意の機能を検討できる状態で残し、採否と接続作業を別に判断する。
