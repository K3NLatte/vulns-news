# vulns-news

<!-- impeccable:product-schema 1 -->

## Platform
web

## Stack
Vue + TypeScript in frontend/. Go backend in the same repository.

## Users
PSIRT、メンテナー、脅威インテリジェンスなどのセキュリティ従事者。

## Product Purpose
一般的な脆弱性情報と、公開リポジトリに関連する情報を閲覧し、影響・根拠・対応方針を確認する。

## Capabilities and Constraints
完成形を議論するフル機能モック。一般・リポジトリ別フィード、検索、並び替え、CVSS、リポジトリへの影響と対応優先度、記事ページ、複数リポジトリ設定、ログイン操作、保存、コメント、対応状況を含む。PoCは静的な文字列として読み、実行しない。

解析の段階と結果の状態を分ける。「分析済み／未確定」で絞り込み、未確定の記事には確定した分析本文や対応手順を示さない。既存のanalysisクエリは待ち・処理中・完了・失敗を切り替える固定プレビューで、自動完了しない。

追加案は、利用者が提案した「脆弱性情報版VirusTotal」の操作検討であり、チーム合意済みの製品要件ではない。CVE・GHSA・公開アドバイザリURLの入力、既存レポートの再利用、新規依頼、再分析、期限付き追跡、過去情報を含むリポジトリ検索、記事リンクの共有をフル版の#/analyzeと記事画面で試せる。新規依頼は約6秒で状態が進む別のモック処理で、クロールやLLMは実行しない。

追跡7日・確認間隔1日は暫定値。期限終了後も既存レポートは読める。手動の再分析と追跡の再開を別操作にし、再分析だけでは期限を延長しない。新着の追加候補は利用者が反映するまで閲覧中の一覧を変えない。

MVPは同じ部品から、閲覧、検索、重要度・関連度順、単一リポジトリ入力、記事詳細を提供する。実装をコピーして分岐させない。

取得・解析・認証・公開コメント・DB永続化は未接続。ブラウザー内で操作と状態を再現する。ゲストはsessionStorage、ログイン後のプロフィールはlocalStorageに保持し、実際の認証とは区別する。 個別解析の依頼と追跡履歴は同じタブのsessionStorageに保持する。共有は記事URLの受け渡しで、公開サーバーへのレポート保存やアップロードではない。

画面案を基準版として共有する。以後の処理・入力検証・保存状態の修正は基準版から分けたブランチでレビューする。マージは行わない。追加の個別解析構想はチーム未合意のままとする。

## Brand Commitments
日本語UI。白・黒・グレーを基調とし、青を操作と選択に用いる。重要度と対応優先度には赤・橙・黄・緑を使い、ラベルを併記する。ユーザー指定のカンザキイオリ／amazarashiのモチーフは、紙面の余白と文字の強弱へ抽象化する。歌詞、ロゴ、写真、既存レイアウトを流用しない。

可用性と利便性を優先する。本文・入力16px、一覧タイトル18px、補助情報14pxを基本とする。左の一覧は通常の文書スクロールとし、高さ制限を設けない。PCの右詳細は追従し、操作・ID・タイトル・CVSSを残して本文だけを独立スクロールする。並列表示に必要な画面高は標準16pxで500pxとし、ルート文字サイズの拡大率に応じて増やす。ヘッダーと上部操作域は固定しない。モック注意書き、キャッチコピー、実装事情を画面へ散在させない。

## Evidence on Hand
標準フィードと過去レポートの本文は固定の架空データ。リポジトリ別の調査は固定プロフィールと過去レポート2件を使い、実際のリポジトリや過去の全情報を取得しない。新規入力の存在・製品・バージョン・CVSSは、モック処理が完了しても未確認・未評価のままとする。

useAnalysisPreviewの固定状態と、useReportLibraryの時間経過による操作再現は別の仕組み。どちらもバックエンドAPIの契約ではない。定期確認は画面が開いている間のローカル処理であり、サーバーの定期ジョブではない。localhostの共有URLや同じタブだけに存在する新規レポートを、他端末から利用できる共有サービスとして扱わない。実装境界の説明はREADMEとレビュー文書へ集約する。

## Product Principles
- CVSS、リポジトリ関連度、対応優先度、分析の確度、分析済みかどうかを混同しない。
- 不明な件数・進捗率・関連度を推測して埋めない。
- 記事本文と外部入力は文字列として表示する。
- UI、状態保存、データ取得を分け、レビューで責務を追える構造にする。
