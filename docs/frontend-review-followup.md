# レビュー後の修正状況

対象: `codex/frontend-review-fixes`。比較先: `codex/frontend-review-suite` (`ba0da65`)。
この差分は優先度の高い不具合と、その周辺の再現済み問題を扱います。全指摘の解決、本番投入可、マージ可を意味しません。

## 今回の変更

| 指摘 | 変更 | 主な確認箇所 |
| --- | --- | --- |
| H-1 / A2 | 記事の存在と現在のリポジトリの評価を分離。評価がない・未確定の記事では関連度、依存情報、対応状況編集を表示しない | `repositoryAssessment.ts`、`FeedDetail.vue`、`review-regressions.spec.ts` |
| H-2 / B2 | 解析履歴・入力URL・追加記事・追跡を利用者別に切替。旧タイマーと監視を停止し、直前の変更も旧所有者に保存 | `useReportLibrary.ts`、同単体テスト、プロフィール切替E2E |
| H-3 | 開発用URLスイッチをDEVビルド限定に変更。本番の空件数・失敗・MVPへの切替を無効化 | `previewOptions.ts`、単体テスト、`preview.production.spec.ts` |
| M-1・M-2 | 閲覧時の自動解析を削除。明示的な検索操作と未開始・拒否・待機・失敗・中止・完了の表示 | `App.vue`、検索受付上限のE2E |
| M-6・M-7 / A1 | 不明な評価は未確定へ。並び替え前に分類し、未確定は関連度順の末尾へ | `useAnalysisPreview.ts`、`feed.ts`、単体・E2E |
| M-10 / B1 | 未変更のプロフィールは再ログイン時に再読込。本当に未保存の変更は保持 | `workspace.ts`、別タブ更新と未保存変更の単体テスト |
| M-12・M-13 | 表示名の正規化を安定化。プロフィールやログイン状態の保存失敗時はログインを確定せず、ゲスト内容を保持 | `workspace.test.ts` |
| M-15 / B4 | コメントエラーをオブジェクトの複製で消さない | 制御文字を含む投稿のE2E |
| C2（先行レビュー） | 日本語の変換中は検索しない | compositionイベントのE2E。実IMEは未確認 |
| M-31の一部 / C8 | 共有パネルのEscapeをパネル内に限定。背面からログインダイアログを妨害しない | 共有失敗→ログイン→EscapeのE2E |

## 確認手順と結果

1. 設定で `https://github.com/example/backend` を登録。記事 `demo-001` をこのリポジトリ文脈で開くと未確定、`demo-002` では評価と対応状況編集が表示される。
2. aliceで解析依頼→ログアウト→bobへ切替。bobからaliceの履歴・追加記事を閲覧できず、aliceへ戻すと復元する。
3. `pnpm build` 後に `pnpm test:e2e:production` を実行。`?analysis=completed&scenario=empty` を付けても通常の結果を表示する。

型チェック、単体テスト255件、本番ビルドは成功。開発時E2EはChromium・Firefoxの58項目を確認しました。一括実行は57件成功・1件失敗で、Firefoxの入力補助が制御文字を変換していたため、そのテストを同じ不正値の直接注入に直し、両ブラウザーで再実行して成功しました。本番成果物へのE2Eは2件成功。開発時のE2Eと本番成果物のE2Eは別の設定です。

```bash
pnpm typecheck
pnpm test
pnpm build
pnpm test:e2e
pnpm test:e2e:production
```

## 残る指摘と判断事項

- **表示の正確さ**: CVSS未評価の尺度、追加入力の依頼日時と公開日時、初回分析・未分析・追跡の区別、参照URLと識別用URLの区別（M-4・M-5・M-8、A3・A4・A6）。
- **保存・復旧**: 真の競合時の復旧導線、保存失敗後の成功表示、部分破損の復旧、入力正規化後の長さ、履歴上限・時計変更・孤立データ（M-11・M-14・M-16〜M-21）。自動で壊れたデータを削除する方針にはしていません。
- **取得・API**: 再取得中の結果保持、例外の分類、非同期更新、サーバー検索・ページングへの移行（M-22〜M-26）。Go APIの契約を確認して別差分で扱います。
- **操作・アクセシビリティ**: 複数回の戻る・進む、選択保持、消える操作のフォーカス、共有パネルの重なり、通知・文字サイズ設定・コントラスト。Escapeの修正だけでM-31全体を解決とはしません（M-27〜M-37）。
- **仕様**: ゲスト内容の移行方法、履歴に保存するURLのクエリ、未合意の分析機能の採否と分離。本人確認・サーバー側の認可は未実装です。
- **未確認**: 実バックエンド、iOS/Safari・WebKit、スクリーンリーダー、実IME、本番配信ヘッダー。既存の自動試験の成功は、これらの確認を代替しません。

旧レビュー資料の結果は当時の対象ケースに限ります。プロフィール分離、キーボード、未評価表示をアプリ全体で確認済みと読める記述は、今回の修正表と残件を優先してください。
